import type { NextConfig } from 'next';

// Vercel baut Next.js nativ — kein `output: 'standalone'` nötig (das ist für
// Self-Hosting/Docker). Konfiguration bewusst minimal halten.
const nextConfig: NextConfig = {};

export default nextConfig;
